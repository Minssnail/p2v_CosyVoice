# PPT语音视频生成系统 - 完整概要设计文档

## 1. 项目概述

### 1.1 系统简介
本系统是一个基于AI技术的PPT自动生成视频平台，通过集成CosyVoice语音合成技术，能够将PowerPoint演示文稿自动转换为带有语音讲解的高清视频。系统采用Flask Web框架，支持用户自定义音色克隆，实现多实例并行处理，大幅提升视频生成效率。

### 1.2 核心特性
- **零样本语音克隆**：支持用户上传5-10秒音频样本，自动克隆个人音色
- **多实例并行处理**：支持多台CosyVoice服务器并发工作，智能负载均衡
- **GPU硬件加速**：使用NVIDIA GPU加速视频渲染（NVENC编码）
- **实时进度反馈**：基于Server-Sent Events (SSE)的实时进度推送
- **用户权限隔离**：每个用户的音色和数据完全隔离，确保安全性
- **多种视频模式**：演播室模式（带背景）和标准全屏模式

### 1.3 技术栈
| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | HTML5 + Tailwind CSS + Vanilla JS | 响应式UI，无需构建工具 |
| 后端框架 | Flask | 轻量级Web框架 |
| 数据库 | SQLite3 (WAL模式) | 零配置，支持并发读 |
| 语音合成 | CosyVoice2-0.5B | 零样本克隆，FP16精度 |
| 备选TTS | Edge TTS | 系统默认音色（无GPU） |
| 视频处理 | FFmpeg + h264_nvenc | GPU硬件加速编码 |
| PPT解析 | PowerPoint COM API | 提取备注和导出图片 |
| Web服务器 | Waitress | 生产级WSGI服务器 |
| 并发控制 | asyncio + threading | 异步I/O + 后台线程 |

---

## 2. 项目目录结构

```
E:\p2v_CosyVoice/
├── app.py                          # Flask主程序（399行）
├── ppt2video_engine.py             # 核心视频生成引擎（1057行）
├── db.py                           # 数据库操作层（169行）
├── run.py                          # 生产服务器启动脚本
├── requirements.txt                # Python依赖清单
│
├── data/
│   └── p2v.db                     # SQLite数据库文件
│
├── static/
│   ├── assets/
│   │   └── bg_tech.png            # 演播室模式背景图
│   ├── uploads/                   # 用户上传文件（PPT、音频）
│   │   ├── voice_*.wav            # 音色样本文件
│   │   └── *_*.pptx               # 上传的PPT文件
│   └── outputs/                   # 生成的视频文件
│       └── *_output.mp4
│
├── templates/
│   ├── login.html                 # 登录/注册页面（109行）
│   ├── index.html                 # 主界面（548行）
│   └── preview.html               # 视频预览页面（34行）
│
├── test/
│
├── .git/                          # Git版本控制
├── .claude/                       # Claude配置
│
├── check_and_fix_audio.py         # 音频修复工具
├── diagnose_performance.py        # 性能诊断工具
├── extract_notes_and_generate.py  # 备注提取工具
├── generate_missing_audio.py      # 音频生成工具
├── render_all_videos.py           # 批量渲染工具
├── test_cosyvoice_api.py          # API测试工具
│
├── walkthrough.md                 # 技术架构文档
├── 系统说明书.md                   # 中文系统说明书
├── COSYVOICE_TIMEOUT_IMPROVEMENTS.md  # 超时改进文档
├── VOICE_REGISTRATION_FIX.md      # 音色注册修复文档
├── ajax_analysis.md               # AJAX分析文档
└── README.md                      # 项目说明
```

**统计信息**：
- Python文件：11个
- 前端文件：3个
- 总代码行数：约2500行（核心文件）

---

## 3. 核心模块详细设计

### 3.1 app.py - Flask Web应用主程序

**文件位置**：`E:\p2v_CosyVoice\app.py`
**代码行数**：399行
**主要职责**：
- HTTP请求路由处理
- 用户会话管理（session-based authentication）
- 音色CRUD API
- 视频生成任务调度
- SSE实时进度推送

#### 3.1.1 全局配置

```python
app = Flask(__name__)
app.secret_key = 'SECRET_KEY_REMOVED'  # 固定密钥，重启不丢失session

BASE_DIR = os.path.abspath(os.path.dirname(__file__))
UPLOAD_FOLDER = os.path.join(BASE_DIR, 'static', 'uploads')
OUTPUT_FOLDER = os.path.join(BASE_DIR, 'static', 'outputs')

# 存储正在运行的任务
_tasks = {}
```

#### 3.1.2 路由设计

| 路由 | 方法 | 功能 | 认证 |
|------|------|------|------|
| `/login` | GET/POST | 用户登录 | 否 |
| `/register` | POST | 用户注册 | 否 |
| `/logout` | GET | 用户登出 | 是 |
| `/` | GET/POST | 主页面/生成任务 | 是 |
| `/api/voices` | GET | 获取音色列表 | 是 |
| `/api/voices/create` | POST | 创建新音色 | 是 |
| `/api/voices/delete` | POST | 删除音色 | 是 |
| `/api/progress/<session_id>` | GET | SSE进度流 | 是 |
| `/preview/<filename>` | GET | 预览视频 | 是 |
| `/download/<filename>` | GET | 下载视频 | 是 |

#### 3.1.3 音频处理工具

**功能**：将各种音频格式转换为CosyVoice要求的标准WAV格式（16kHz, mono, PCM 16-bit）

```python
def convert_to_wav(input_path, output_path=None):
    """
    支持: m4a, mp3, ogg → WAV (16kHz, mono)
    - 如果已经是标准格式，直接返回
    - 使用FFmpeg进行转换
    - 自动处理输出文件名冲突
    """
```

**特点**：
- 自动检测输入格式
- 智能跳过已符合标准的WAV文件
- 30秒超时保护
- 详细错误提示

#### 3.1.4 认证装饰器

```python
def login_required(f):
    @wraps(f)
    def wrapper(*args, **kwargs):
        if 'user_id' not in session:
            return redirect(url_for('login'))
        return f(*args, **kwargs)
    return wrapper
```

#### 3.1.5 音色管理核心流程

**创建音色流程**：
```python
@app.route('/api/voices/create', methods=['POST'])
@login_required
def create_voice():
    # 1. 验证输入参数
    voice_name = request.form.get('voice_name', '').strip()
    prompt_text = request.form.get('prompt_text', '').strip()

    # 2. 生成唯一的speaker_id
    speaker_id = db.make_speaker_id(user_id, voice_name)  # u{user_id}_{hash}

    # 3. 保存并转换音频
    prompt_path = convert_to_wav(temp_path)

    # 4. 并发注册到所有CosyVoice实例
    for api_url in live_urls:
        requests.post(f"{api_url}/api/speakers/register", ...)

    # 5. 归档标准WAV文件
    archived_wav_path = os.path.join(UPLOAD_FOLDER, f"voice_{speaker_id}_std.wav")

    # 6. 写入数据库
    voice = db.add_voice(user_id, voice_name, speaker_id, prompt_text)
```

**音色ID命名规则**：
- 用户自定义：`u{user_id}_{md5_hash}`（如：`u2_a638102f`）
- 临时克隆：`p2v_{session_id}`（任务结束后自动清理）
- 系统默认：`zh-CN-XiaoxiaoNeural`（Edge TTS）

#### 3.1.6 SSE实时进度推送

```python
@app.route('/api/progress/<session_id>')
def progress_stream(session_id):
    def event_stream():
        while True:
            prog = get_progress(session_id)
            yield f"data: {json.dumps(prog, ensure_ascii=False)}\n\n"
            if prog.get("done"):
                clear_progress(session_id)
                break
            time.sleep(0.8)

    return Response(event_stream(), mimetype='text/event-stream',
                    headers={'Cache-Control': 'no-cache'})
```

**进度数据结构**：
```python
{
    "stage": "parse",      # parse, tts, render, merge, done, error
    "current": 5,
    "total": 10,
    "detail": "正在解析PPT",
    "done": False,
    "success": True,
    "redirect": "/preview/xxx.mp4"  # 完成后的跳转链接
}
```

---

### 3.2 db.py - 数据库操作层

**文件位置**：`E:\p2v_CosyVoice\db.py`
**代码行数**：169行
**主要职责**：
- 数据库连接管理（WAL模式）
- 用户CRUD操作
- 音色CRUD操作
- SQL注入防护

#### 3.2.1 数据库表设计

**users表**（用户表）：
```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,        -- 用户名（登录凭证）
    password_hash TEXT NOT NULL,          -- SHA256密码哈希
    display_name TEXT DEFAULT '',         -- 显示名称
    created_at REAL DEFAULT (strftime('%s','now'))  -- Unix时间戳
);
```

**user_voices表**（音色表）：
```sql
CREATE TABLE user_voices (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,             -- 所属用户ID
    voice_name TEXT NOT NULL,             -- 音色名称
    cosyvoice_speaker_id TEXT UNIQUE NOT NULL,  -- CosyVoice说话人ID
    prompt_text TEXT DEFAULT '',          -- 提示文本（音频中的原话）
    created_at REAL DEFAULT (strftime('%s','now')),
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);
```

**数据库优化配置**：
```python
conn.execute("PRAGMA journal_mode=WAL")   # 写前日志，支持并发读
conn.execute("PRAGMA foreign_keys=ON")    # 启用外键约束
```

#### 3.2.2 核心API

**用户操作**：
```python
create_user(username, password, display_name) -> dict  # 创建用户
verify_user(username, password) -> dict | None        # 验证登录
get_user_by_id(user_id) -> dict | None                # 获取用户信息
```

**音色操作**：
```python
make_speaker_id(user_id, voice_name) -> str          # 生成唯一ID
add_voice(user_id, voice_name, speaker_id, prompt_text) -> dict
get_user_voices(user_id) -> list[dict]                # 包含归档WAV路径
delete_voice(user_id, voice_id) -> str | None         # 返回speaker_id
get_voice_by_speaker_id(user_id, speaker_id) -> dict  # 权限验证
```

**安全特性**：
- 密码使用SHA256哈希存储
- 参数化查询防止SQL注入
- 级联删除（用户删除时自动删除其音色）

---

### 3.3 ppt2video_engine.py - 核心视频生成引擎

**文件位置**：`E:\p2v_CosyVoice\ppt2video_engine.py`
**代码行数**：1057行
**主要职责**：
- PPT解析和图片提取
- CosyVoice多实例调度
- 语音合成并发控制
- 视频渲染和合并
- 进度管理

#### 3.3.1 配置参数

```python
# TTS提供商选择
TTS_PROVIDER = "cosyvoice"  # cosyvoice | azure | edge

# CosyVoice多服务器配置
COSYVOICE_SERVERS = [
    {"host": "YOUR_SERVER_IP", "port_range": (9880, 9888)},  # 9个实例
    # {"host": "YOUR_SERVER_IP_2", "port_range": (50050, 50055)},  # 可扩展
]

# 并发控制
MAX_TTS_CONCURRENT = 1  # 动态调整（等于实例数）
MAX_RENDER_CONCURRENT = 8

# 背景图和布局
BACKGROUND_IMAGE_PATH = 'static/assets/bg_tech.png'
SCREEN_LAYOUT = {"x": 38, "y": 66, "w": 990, "h": 558}
```

#### 3.3.2 CosyVoice实例自动发现

```python
async def _discover_cosyvoice_instances():
    """
    两步发现策略：
    1. TCP端口扫描（快速筛选）
    2. API健康检查（验证服务可用性）
    返回：真正可用的实例URL列表
    """
```

**特点**：
- 并发TCP扫描（1秒超时）
- API健康检查（10秒超时）
- 自动剔除故障实例
- 启动时动态发现，无需手动配置

#### 3.3.3 音色注册和同步

**预注册音色**（临时克隆）：
```python
async def _register_zero_shot_speaker(session_id, prompt_wav, prompt_text, api_urls):
    speaker_id = f"p2v_{session_id}"
    # 并发注册到所有实例
    tasks = [_register_single(api_url) for api_url in api_urls]
    await asyncio.gather(*tasks)
    return speaker_id
```

**音色验证和同步**：
```python
async def _verify_speaker_on_all_instances(speaker_id, api_urls):
    """检查音色是否在所有实例上存在"""
    available_count = sum(results)
    if available_count < len(api_urls):
        # 如果缺少且有原始音频，自动重新分发
        await _register_zero_shot_speaker(...)
```

#### 3.3.4 长文本智能分段

```python
def _split_long_text(text, max_length=200):
    """
    智能分段策略：
    1. 优先在句子边界切分（。！？；）
    2. 每段50-200字符
    3. 保留语义完整性
    4. 超长文本强制按字符切分
    """
```

**示例**：
```
输入：300字的长文本
输出：["150字...", "150字..."]  # 2段
```

#### 3.3.5 多实例负载均衡和故障转移

**轮询调度**（Round-Robin）：
```python
global _tts_round_robin_counter
primary_idx = _tts_round_robin_counter % len(available_urls)
_tts_round_robin_counter += 1

# 构建轮询队列：[当前实例, 其他实例]
rotated_urls = [available_urls[primary_idx]] + [其他实例]
```

**智能故障转移**：
```python
# 区分"繁忙"与"真实故障"
failed_instances = set()  # HTTP 4xx/5xx错误，永久禁用
busy_instances = set()    # 超时，临时标记

for api_url in rotated_urls:
    try:
        result = await _try_single_instance(..., timeout=300)
        if result:
            busy_instances.discard(api_url)  # 成功后移除繁忙标记
            return True
    except asyncio.TimeoutError:
        busy_instances.add(api_url)  # 临时标记为繁忙
    except HTTPError:
        failed_instances.add(api_url)  # 永久标记为故障
```

#### 3.3.6 TTS并发处理（分段级）

```python
# 分段并发：始终保持 max_concurrent 个分段在处理
max_concurrent = len(available_urls)
segment_semaphore = asyncio.Semaphore(max_concurrent)

async def _do_segment_tts(page_idx, page_num, seg_idx, seg_text, slide_data):
    async with segment_semaphore:
        # 轮询分配实例
        # 故障转移
        # 返回分段音频路径

# 启动所有分段任务
segment_coros = [_do_segment_tts(...) for ... in all_segments]
segment_results = await asyncio.gather(*segment_coros)
```

**性能优化**：
- 并发数 = 实例数（充分利用所有实例）
- 工作池模式（一个完成立即启动下一个）
- 分段级故障转移（不影响其他分段）

#### 3.3.7 音频分段合并

```python
# 收集每个页面的所有分段
page_segments = defaultdict(list)
for page_idx, seg_idx, seg_file, success in segment_results:
    page_segments[page_idx].append((seg_idx, seg_file))

# 使用ffmpeg concat协议合并
list_file = f"merge_p{page_idx}.txt"
with open(list_file, "w") as f:
    for seg_idx, seg_file in segments_list:
        f.write(f"file '{os.path.abspath(seg_file)}'\n")

cmd = ["ffmpeg", "-f", "concat", "-safe", "0", "-i", list_file, target_file]
await asyncio.create_subprocess_exec(*cmd)
```

#### 3.3.8 视频渲染（双模式）

**模式一：演播室模式**（带背景叠加）：
```python
filter_complex = (
    f"[1:v]scale={w}:{h},setsar=1,{raw_effect_filter}[ppt];"
    f"[0:v][ppt]overlay=x={x}:y={y}:shortest=1[outv]"
)
cmd = [
    "ffmpeg", "-loop", "1", "-i", BACKGROUND_IMAGE_PATH,  # 背景图
    "-loop", "1", "-i", img_path,                         # PPT图
    "-i", audio_path,
    "-filter_complex", filter_complex,
    "-map", "[outv]", "-map", "2:a",
    "-c:v", "h264_nvenc", "-preset", "p1", "-r", "24",
    output_video_path
]
```

**模式二：标准全屏**（无背景）：
```python
cmd = [
    "ffmpeg", "-loop", "1", "-i", img_path,  # 只有PPT
    "-i", audio_path,
    "-vf", raw_effect_filter,  # 直接应用滤镜
    "-c:v", "h264_nvenc", "-preset", "p1", "-r", "24",
    output_video_path
]
```

**转场效果**：
- 淡入淡出（fade）
- 模糊渐变（blur）
- 随机选择

#### 3.3.9 进度管理

```python
_progress_store = {}  # {session_id: progress_dict}
_progress_lock = threading.Lock()

def update_progress(session_id, stage, current=0, total=0, detail="", done=False, success=True):
    """线程安全的进度更新"""
    with _progress_lock:
        _progress_store[session_id] = {...}

def get_progress(session_id):
    """获取当前进度（返回副本，避免并发问题）"""
    with _progress_lock:
        return _progress_store.get(session_id, {...}).copy()
```

**进度阶段**：
- `init`: 任务初始化
- `parse`: PPT解析（10%权重）
- `tts`: 语音合成（50%权重）
- `render`: 视频渲染（30%权重）
- `merge`: 视频合并（10%权重）
- `done`: 完成
- `error`: 出错

---

## 4. 前端设计

### 4.1 技术栈
- **UI框架**：Tailwind CSS (CDN)
- **JavaScript**：Vanilla JS（无框架）
- **通信方式**：AJAX (POST) + SSE (Server-Sent Events)
- **字体**：Inter (Google Fonts)

### 4.2 页面结构

#### 4.2.1 login.html - 登录/注册页面

**文件位置**：`E:\p2v_CosyVoice\templates\login.html`
**代码行数**：109行

**功能**：
- 双标签切换（登录/注册）
- 表单验证（用户名≥2字符，密码≥4字符）
- 错误提示（红色警告框）
- 成功提示（绿色提示框）

**UI设计**：
- 渐变背景（blue-50 → purple-50）
- 圆角卡片（rounded-3xl）
- 图标化Logo

#### 4.2.2 index.html - 主界面

**文件位置**：`E:\p2v_CosyVoice\templates\index.html`
**代码行数**：548行

**核心组件**：

1. **用户栏**：
   - 显示用户名
   - 退出登录链接

2. **文件上传区域**：
   - 拖拽上传
   - 点击选择文件
   - 文件名显示
   - 只接受.pptx格式

3. **音色选择区域**：
   - 系统默认音色（2个）
   - 用户自定义音色（动态加载）
   - 创建新音色按钮
   - 临时克隆选项

4. **零样本克隆配置**（动态显示）：
   - 上传参考音频
   - 输入参考文本

5. **视频模式选择**：
   - 演播室模式（默认，带背景）
   - 标准全屏（无背景）

6. **进度面板**（模态框）：
   - 阶段指示器（1-2-3-4）
   - 进度条（动画效果）
   - 百分比显示
   - 实时状态文字

7. **创建音色弹窗**：
   - 音色名称输入
   - 参考文本输入
   - 音频文件上传
   - 实时状态反馈

**JavaScript核心逻辑**：

```javascript
// SSE监听进度
const evtSource = new EventSource(`/api/progress/${sessionId}`);
evtSource.onmessage = (event) => {
    const data = JSON.parse(event.data);
    updateProgressUI(data);
    if (data.done) {
        evtSource.close();
    }
};

// 动态更新进度UI
function updateProgressUI(data) {
    const stageWeights = {parse: 10, tts: 50, render: 30, merge: 10};
    let overallPercent = calculatePercent(data.stage, data.current, data.total);

    // 更新进度条、阶段指示器、文字
    document.getElementById('progress-bar').style.width = overallPercent + '%';

    // 完成后自动跳转
    if (data.done && data.success) {
        setTimeout(() => {
            window.location.href = data.redirect;
        }, 1500);
    }
}
```

#### 4.2.3 preview.html - 视频预览页面

**文件位置**：`E:\p2v_CosyVoice\templates\preview.html`
**代码行数**：34行

**功能**：
- HTML5视频播放器
- 下载按钮
- 返回首页链接

---

## 5. 数据流和业务流程

### 5.1 用户注册和登录流程

```
用户访问系统 → 检查session → 未登录 → 重定向到/login
    ↓
填写注册信息 → POST /register → db.create_user()
    ↓
写入users表 → SHA256密码哈希 → 返回成功提示
    ↓
用户登录 → POST /login → db.verify_user()
    ↓
验证通过 → 设置session['user_id'] → 重定向到/
```

### 5.2 创建自定义音色流程

```
用户上传音频 + 填写信息 → POST /api/voices/create
    ↓
生成speaker_id: u{user_id}_{hash}
    ↓
convert_to_wav() → FFmpeg转换 → 16kHz WAV
    ↓
并发注册到所有CosyVoice实例
    ├─ 实例1: POST /api/speakers/register
    ├─ 实例2: POST /api/speakers/register
    └─ 实例N: POST /api/speakers/register
    ↓
归档标准WAV: voice_{speaker_id}_std.wav
    ↓
写入user_voices表 → 返回成功
```

### 5.3 PPT视频生成完整流程

```
用户上传PPT + 选择音色 → POST /
    ↓
保存文件 → 生成session_id
    ↓
启动后台线程 → run_generation()
    ↓
【阶段1】解析PPT
    ├─ PowerPoint COM API → 打开PPT
    ├─ 遍历幻灯片 → Export PNG (1920x1080)
    └─ 提取备注文本 → notes_slide.notes_text_frame.text
    ↓
【阶段1.5】音色预注册（仅临时克隆）
    └→ 并发注册p2v_{session_id}到所有实例
    ↓
【阶段2】语音合成（并发）
    ├─ 预分段：长文本 → 200字/段
    ├─ 轮询分配：段N → 实例(N % 实例数)
    ├─ 故障转移：主实例失败 → 尝试备用实例
    ├─ 并发控制：Semaphore(实例数)
    └─ 分段合并：ffmpeg concat协议
    ↓
【阶段3】视频渲染（并发）
    ├─ 并发控制：Semaphore(8)
    ├─ 演播室模式：背景图 + PPT叠加
    └─ 标准模式：PPT全屏
    ↓
【阶段4】视频合并
    └→ ffmpeg concat协议 → 最终视频
    ↓
清理临时文件 → 注销临时音色
    ↓
更新进度: done=True → SSE发送完成信号
    ↓
前端自动跳转到 /preview/{filename}
```

### 5.4 多实例负载均衡流程

```
任务启动 → _discover_cosyvoice_instances()
    ↓
TCP扫描 9880-9888端口 → 筛选开放端口
    ↓
API健康检查 → 验证服务可用性
    ↓
COSYVOICE_API_URLS = [http://YOUR_SERVER_IP:9880, ...]
    ↓
动态调整并发数: MAX_TTS_CONCURRENT = len(Urls)
    ↓
【分段级负载均衡】
for segment in segments:
    ├─ 轮询获取主实例: _tts_round_robin_counter % len(Urls)
    ├─ 尝试主实例 → 失败 → 标记故障/繁忙
    ├─ 尝试备用实例 → 按顺序遍历
    └─ 全部失败 → 记录到tts_failed
    ↓
【故障恢复机制】
    ├─ 繁忙实例（超时）: 临时禁用，下次重试
    └─ 故障实例（HTTP错误）: 永久禁用
```

---

## 6. 模块间调用关系

```
┌─────────────────────────────────────────────────────────────┐
│                         前端浏览器                          │
│  (login.html, index.html, preview.html)                    │
└─────────────────────┬───────────────────────────────────────┘
                      │ HTTP/SSE
                      ▼
┌─────────────────────────────────────────────────────────────┐
│                       app.py (Flask)                        │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │ 认证路由  │  │  音色API     │  │  任务调度API     │     │
│  └──────────┘  └──────────────┘  └────────┬─────────┘     │
└─────────────────────────────────────┼───────────────────────┘
                                      │ threading.Thread
                                      ▼
┌─────────────────────────────────────────────────────────────┐
│              ppt2video_engine.py (异步引擎)                │
│  ┌──────────┐  ┌──────────────┐  ┌──────────────────┐     │
│  │ PPT解析  │  │ TTS并发调度  │  │  视频渲染        │     │
│  └────┬─────┘  └──────┬───────┘  └────────┬─────────┘     │
└───────┼───────────────┼──────────────────┼─────────────────┘
        │               │                  │
        ▼               ▼                  ▼
┌─────────────┐ ┌─────────────────┐ ┌──────────┐
│PowerPoint   │ │CosyVoice集群    │ │ FFmpeg   │
│COM API      │ │(9880-9888)      │ │ NVENC    │
└─────────────┘ └─────────────────┘ └──────────┘
                    ▲
                    │ HTTP API
                    │
┌───────────────────┴─────────────────────────────────────┐
│              db.py (SQLite数据库)                       │
│  ┌────────────┐         ┌──────────────────┐           │
│  │ users表    │         │ user_voices表     │           │
│  └────────────┘         └──────────────────┘           │
└──────────────────────────────────────────────────────────┘
```

**调用链详细说明**：

1. **用户登录流程**：
   ```
   login.html → POST /login → app.login() → db.verify_user() → SQLite
   ```

2. **创建音色流程**：
   ```
   index.html → POST /api/voices/create → app.create_voice()
     → convert_to_wav() → FFmpeg
     → requests.post() → CosyVoice API × N
     → db.add_voice() → SQLite
   ```

3. **生成视频流程**：
   ```
   index.html → POST / → app.index()
     → threading.Thread() → run_generation()
       → ppt_to_images() → PowerPoint COM API
       → _discover_cosyvoice_instances() → TCP扫描
       → _generate_cosyvoice() → CosyVoice API × N
       → render_slide_video() → FFmpeg NVENC
       → subprocess.run() → FFmpeg concat
     → SSE进度推送 → index.html (EventSource)
   ```

---

## 7. 配置文件和依赖

### 7.1 requirements.txt

```
flask              # Web框架
waitress           # WSGI服务器
requests           # HTTP客户端
edge-tts           # 备选TTS
python-pptx        # PPT解析
pywin32            # Windows COM API
azure-cognitiveservices-speech  # Azure TTS（可选）
```

### 7.2 核心配置参数

**app.py**：
```python
app.secret_key = 'SECRET_KEY_REMOVED'
UPLOAD_FOLDER = 'static/uploads/'
OUTPUT_FOLDER = 'static/outputs/'
```

**ppt2video_engine.py**：
```python
TTS_PROVIDER = "cosyvoice"
COSYVOICE_SERVERS = [
    {"host": "YOUR_SERVER_IP", "port_range": (9880, 9888)},
]
MAX_RENDER_CONCURRENT = 8
BACKGROUND_IMAGE_PATH = 'static/assets/bg_tech.png'
SCREEN_LAYOUT = {"x": 38, "y": 66, "w": 990, "h": 558}
```

**db.py**：
```python
DB_PATH = 'data/p2v.db'
```

### 7.3 环境依赖

**软件要求**：
- Python 3.8+
- Microsoft PowerPoint 2019/2021/365
- FFmpeg 4.0+（支持NVENC）
- NVIDIA GPU驱动 470+
- CUDA 12.4

**硬件要求**：
- CPU: 4核+
- RAM: 16GB+（推荐32GB）
- GPU: NVIDIA GPU（8GB+ VRAM，推荐A40 48GB）
- 存储: 100GB+ SSD

---

## 8. 静态资源和模板文件

### 8.1 static/目录结构

```
static/
├── assets/
│   └── bg_tech.png          # 演播室模式背景图（1920x1080）
├── uploads/
│   ├── voice_*.wav          # 音色样本（归档）
│   ├── voice_*_std.wav      # 标准WAV（16kHz）
│   └── *_*.pptx             # 用户上传的PPT文件
└── outputs/
    └── *_output.mp4         # 生成的视频文件
```

### 8.2 templates/目录结构

```
templates/
├── login.html               # 登录/注册页面（109行）
├── index.html               # 主界面（548行）
└── preview.html             # 视频预览页面（34行）
```

### 8.3 前端技术细节

**Tailwind CSS**：
- CDN引入：`<script src="https://cdn.tailwindcss.com"></script>`
- 无需构建工具
- 响应式设计

**关键UI组件**：
- 进度面板：模态框 + 动画效果
- 音色卡片：网格布局 + 悬停效果
- 文件上传：拖拽区域 + 实时反馈

**JavaScript特性**：
- SSE实时通信
- FormData异步提交
- DOM动态操作（创建音色卡片）
- 轮询进度更新（0.8秒间隔）

---

## 9. 核心业务流程详解

### 9.1 音色权限验证流程

```python
# app.py
if selected_voice.startswith('u'):
    # 用户自定义音色
    voice_info = db.get_voice_by_speaker_id(user_id, selected_voice)
    if not voice_info:
        return jsonify({"error": "音色不存在或无权使用"}), 403
    voice_config = {
        "voice_name": "zero_shot",
        "registered_speaker_id": selected_voice
    }
```

**安全机制**：
- speaker_id前缀检查（`u{user_id}_`）
- 数据库所有权验证
- 无法访问其他用户的音色

### 9.2 CosyVoice实例健康检查

```python
async def _discover_cosyvoice_instances():
    # 第一步：TCP快速扫描
    for port in range(port_start, port_end + 1):
        s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
        s.settimeout(1)
        result = s.connect_ex((host, port))
        if result == 0:
            tcp_alive.append(f"http://{host}:{port}")

    # 第二步：API健康检查
    async def check_api健康(url):
        async with aiohttp.ClientSession() as session:
            try:
                async with session.get(f"{url}/health") as resp:
                    return url, resp.status == 200
            except:
                return url, False

    results = await asyncio.gather(*[check_api健康(url) for url in tcp_alive])
    return [url for url, healthy in results if healthy]
```

### 9.3 智能文本分段逻辑

```python
def _split_long_text(text, max_length=200):
    sentence_endings = ('。', '！', '？', '；', '.', '!', ';', '\n')
    segments = []
    current_segment = ""

    for char in text:
        current_segment += char
        if char in sentence_endings and len(current_segment) >= 50:
            if len(current_segment) >= max_length * 0.8:
                segments.append(current_segment.strip())
                current_segment = ""

    if current_segment.strip():
        segments.append(current_segment.strip())

    return segments
```

**分段策略**：
1. 优先在句子结束符处切分
2. 每段50-200字符
3. 保持语义完整
4. 避免单个词被截断

### 9.4 视频渲染滤镜链

**演播室模式滤镜**：
```
[1:v]scale=990:558,setsar=1,effect[ppt];
[0:v][ppt]overlay=x=38:y=66:shortest=1[outv]
```

**标准模式滤镜**：
```
fade=t=in:st=0:d=0.5,fade=t=out:st={duration-0.5}:d=0.5
```

或：
```
boxblur=... + fade
```

**随机选择**：
```python
effects = ["fade", "blur"]
chosen = random.choice(effects)
```

---

## 10. 性能优化和扩展性

### 10.1 并发控制策略

**TTS并发**：
```python
max_concurrent = len(COSYVOICE_API_URLS)  # 等于实例数
segment_semaphore = asyncio.Semaphore(max_concurrent)
```

**渲染并发**：
```python
MAX_RENDER_CONCURRENT = 8  # 固定8个并发渲染任务
```

**动态调整**：
```python
instance_count = len(COSYVOICE_API_URLS)
max_tts_concurrent = max(instance_count, 1)
```

### 10.2 故障容错机制

**三级容错**：
1. **实例级故障转移**：主实例失败 → 备用实例
2. **超时重试**：最多3次重试，每次间隔5秒
3. **分段隔离**：单个分段失败不影响其他分段

**故障类型处理**：
- 超时：临时标记为繁忙，下次重试
- HTTP错误：永久标记为故障
- 连接失败：跳过该实例

### 10.3 扩展性设计

**水平扩展**：
```python
COSYVOICE_SERVERS = [
    {"host": "YOUR_SERVER_IP", "port_range": (9880, 9888)},
    {"host": "YOUR_SERVER_IP_2", "port_range": (50050, 50055)},
    {"host": "YOUR_SERVER_IP_3", "port_range": (60000, 60010)},
]
```

**自动发现机制**：
- 启动时扫描所有服务器
- 动态获取可用实例
- 无需修改代码

---

## 11. 安全性设计

### 11.1 用户认证

**Session管理**：
```python
session['user_id'] = user['id']
session['display_name'] = user['display_name']
```

**密码加密**：
```python
def _hash_pw(password: str) -> str:
    return hashlib.sha256(password.encode('utf-8')).hexdigest()
```

### 11.2 权限隔离

**音色所有权**：
- speaker_id前缀：`u{user_id}_{hash}`
- 数据库外键约束：`FOREIGN KEY (user_id) REFERENCES users(id)`
- API级验证：`get_voice_by_speaker_id(user_id, speaker_id)`

**文件隔离**：
- 上传文件命名：`voice_{speaker_id}_*.wav`
- 临时文件命名：`{session_id}_*.pptx`

### 11.3 输入验证

**用户名验证**：
```python
if len(username) < 2:
    return "用户名至少2个字符"
```

**文件类型验证**：
```python
input type="file" accept=".pptx"
input type="file" accept="audio/*"
```

---

## 12. 监控和日志

### 12.1 日志级别

**标签系统**：
- `[OK]`: 成功
- `[ERROR]`: 错误
- `[WARN]`: 警告
- `[TIMEOUT]`: 超时
- `[FAIL]`: 失败
- `[RETRY]`: 重试
- `[SUCCESS]`: 成功

**关键日志**：
```python
print(f"[OK] [TTS] 第1页 | audio_1.mp3 | 8.5s | 45字 | :9880")
print(f"[TIMEOUT] 第3页 TTS请求超时，文字长度:350字，重试 1/3")
print(f"[FAILOVER_SUCCESS] 第5页 故障转移成功! (实例2)")
```

### 12.2 性能监控

**时间统计**：
```python
t0 = _time.time()
# ... 执行任务 ...
elapsed = _time.time() - t0
print(f"  [OK] [TTS] ... | {elapsed:.1f}s")
```

**总耗时统计**：
```python
tts_total_elapsed = _time.time() - tts_total_start
print(f"[TIME] [TTS 总耗时] {tts_total_elapsed:.1f}s | {len(tts_items)} 页 | {num_servers} 实例并行")
```

---

## 13. 总结

### 13.1 系统特点

1. **高度集成**：Flask + SQLite + CosyVoice + FFmpeg完整闭环
2. **用户友好**：Web界面、实时进度、拖拽上传
3. **高性能**：多实例并行、GPU加速、智能分段
4. **可扩展**：自动发现实例、动态调整并发
5. **安全性**：权限隔离、密码加密、输入验证

### 13.2 技术亮点

1. **分段级并发**：打破页级并发限制，充分利用所有实例
2. **智能故障转移**：区分繁忙与故障，实现自动恢复
3. **轮询调度**：全局计数器实现真正的负载均衡
4. **SSE实时推送**：0.8秒轮询，用户体验流畅
5. **零样本克隆**：5-10秒音频即可克隆音色

### 13.3 代码质量

- **模块化设计**：app.py、db.py、ppt2video_engine.py职责清晰
- **异步编程**：asyncio + threading，高效并发
- **错误处理**：详细的异常分类和日志
- **代码注释**：中文注释，易于维护
- **文档完善**：walkthrough.md、系统说明书.md

### 13.4 性能指标

| 指标 | 单实例 | 3实例 | 6实例 |
|------|--------|-------|-------|
| 单页TTS（100字） | ~25s | ~25s | ~25s |
| 10页总耗时 | ~230s | ~80s | ~42s |
| 峰值GPU利用率 | ~25% | ~60% | 80-100% |
| 显存占用 (动态) | 4~6.5GB | 12~18GB | 25~30GB |

---

## 14. 附录

### 14.1 关键文件路径

| 文件 | 路径 | 行数 |
|------|------|------|
| Flask主程序 | `E:\p2v_CosyVoice\app.py` | 399 |
| 视频引擎 | `E:\p2v_CosyVoice\ppt2video_engine.py` | 1057 |
| 数据库层 | `E:\p2v_CosyVoice\db.py` | 169 |
| 启动脚本 | `E:\p2v_CosyVoice\run.py` | 42 |
| 登录页面 | `E:\p2v_CosyVoice\templates\login.html` | 109 |
| 主页面 | `E:\p2v_CosyVoice\templates\index.html` | 548 |
| 预览页面 | `E:\p2v_CosyVoice\templates\preview.html` | 34 |
| 数据库 | `E:\p2v_CosyVoice\data\p2v.db` | - |

### 14.2 数据库表总结

| 表名 | 字段数 | 索引 | 外键 |
|------|--------|------|------|
| users | 5 | username (UNIQUE) | - |
| user_voices | 6 | cosyvoice_speaker_id (UNIQUE) | user_id → users(id) |

### 14.3 API路由总结

| 分类 | 路由数 | 主要功能 |
|------|--------|---------|
| 认证 | 3 | 登录、注册、登出 |
| 音色 | 3 | 列表、创建、删除 |
| 任务 | 2 | 提交、进度查询 |
| 文件 | 2 | 预览、下载 |

---

**文档生成时间**：2026-04-29
**系统版本**：基于main分支（commit: c6f4f86）
**总代码行数**：约2500行（核心Python + HTML）
**文档作者**：Claude Code 分析生成
